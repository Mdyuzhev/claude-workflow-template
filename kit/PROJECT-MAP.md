# kit/PROJECT-MAP.md — карта проекта для внешнего участника

> hint: этот файл — скелет. Замени примеры на реальную архитектуру своего
> проекта и удали этот блок после первого заполнения.

> **Анти-протухание.** Перечни, которые растут (модули, номера сценариев),
> несут дату среза и указатель на источник правды в репозитории. Номера
> версий продукта здесь НЕ приводятся — их дом корневой `CLAUDE.md`/
> `CHANGELOG.md` (они меняются каждый релиз; дублировать сюда значит
> гарантировать протухание).

## Что за продукт

<1–3 абзаца: что делает продукт, из каких поверхностей/платформ состоит,
какая из них в активной разработке. Без чисел версий.>

## Архитектура послойно

```
<ASCII- или mermaid-диаграмма слоёв: интерфейс → транспорт/API → ядро →
хранение → тестовый контур. Один блок на слой, ключевые каталоги внутри.>
```

## Анатомия модуля/фичи — сделай аналогично существующим (главный практический раздел)

«Сделай аналогично существующим» — стандартная форма задач в этом репозитории.
Раздел показывает, что именно «аналогично» значит, на примере одного
повторяющегося вида работы (например: добавление новой интеграции, нового
источника данных, нового вида проверки — подставь свой).

### Симметричная цепочка файлов

| Слой | Файл(ы) | Что делает | Учиться по |
|------|---------|------------|------------|
| Состояние | `<путь-паттерн>` | Состояние на единицу работы | `<эталонный существующий модуль>` |
| Интерфейс | `<путь-паттерн>` | UI/CLI-поверхность | `<эталонный существующий модуль>` |
| Швы регистрации | `<файлы, где новая единица регистрируется аддитивно>` | ADDITIVE-only переключение | `kit/PARALLEL-LINES.md` §2 |
| Ядро/бэкенд | `<путь-паттерн>` | Обработка запроса/команды | `<эталонный существующий модуль>` |
| Точка регистрации | `<файл с единственной точкой регистрации хендлеров>` | Аддитивный append в хвост | `<эталонный существующий модуль>` |
| Тестовые идентификаторы | компоненты интерфейса | Префикс по виду — только добавлять (`kit/RULES.md` §4) | `<пример существующего префикса>` |
| Тестовый сценарий | `<реестр сценариев>` + регистрация во всех его копиях | Номер резервируется в `kit/PARALLEL-LINES.md` §1 | `<пример>` |

Дописывай строки таблицы под свой проект — форма (слой → файл → зачем →
эталон) переносится без изменений.

### Сквозной механизм: единый источник состояния

<1 абзац: если в проекте есть единственный helper/источник правды, который
обязаны вызывать все модули определённого вида (например: сборка
конфигурации, каскад настроек) — опиши его здесь: файл, кто вызывает, какое
правило нарушать нельзя.>

### Сквозной механизм: общий визуальный/валидационный эталон

<1 абзац: канонический источник (файл:строка) для повторяющегося паттерна
интерфейса или валидации; запрет на вторую независимую копию того же
регэкспа/правила.>

### Мок-стенд

`<N>` all-or-none переменных окружения — состав и источник правды см.
`kit/STAND.md`.

## Где что лежит: документация и инструменты (срез `<YYYY-MM-DD>`)

<Таблица или список: файл/каталог → назначение. Обнови дату среза при
следующей правке раздела.>

## Ключевые инварианты кода (публично-безопасная проекция)

<Короткий список: 5–10 жёстких правил кода, которые нельзя нарушать
(например: «сборка только обёртками», «состояние — не локальный
компонентный state», «эмодзи вместо иконок запрещены»). Полная версия с
внутренними деталями — в закрытом канон-файле владельца; здесь — безопасная
для публикации проекция.>

## Словарь терминов

| Термин | Значение |
|---|---|
| `<термин проекта>` | `<объяснение>` |

## Карта за минуту (резюме-секция — перечитывается каждой следующей сессией)

Срез `<YYYY-MM-DD>`. Самое важное для старт-ритуала: что активно меняется
прямо сейчас, какие линии/зоны заморожены, где искать последнее решение
владельца. Единственное место для растущих списков — с датой среза и
указателем на машинный источник правды (не дублировать здесь числа,
которые быстро устаревают).
